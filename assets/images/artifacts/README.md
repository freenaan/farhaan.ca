Drop the four flying-artifact images here with these exact filenames — each one is picked up automatically by `assets/js/flying-artifacts.js` with no other code changes needed:

- `pioneer-arm.png`
- `planetary-transmission.png`
- `calculator.png`
- `sumobot.png`

Any file that's missing is skipped silently (no broken-image icon, no error) until it's added.

Source images can be color; the monochrome treatment is applied with a CSS filter at display time. PNG with a transparent background works best.
